# Aero Foundation — VMware / USB

## Build

```powershell
.\tools\build_foundation.ps1
```

Output: `out/AeroOS-Foundation-0.4.0.img` and `.iso`

## VMware

1. Other 64-bit, UEFI
2. Attach `.img` as HDD
3. Setup → Install → Desktop (kernel mode)
4. Keyboard: Space = Start; Files / Store in menu
