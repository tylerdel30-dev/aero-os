# Aero OS repository configuration
# Connected to: https://github.com/tylerdel30-dev/aero-os

# Operating system updates — create a Release tagged like v1.0.1
# Attach: aero-update-manifest.json + updated binaries
os_repo=tylerdel30-dev/aero-os

# App store — .aero packages as Release assets (same repo works fine)
# Optional later: create tylerdel30-dev/aero-store and point apps_repo there
apps_repo=tylerdel30-dev/aero-os

# Catalog on the default branch (optional)
apps_catalog_url=https://raw.githubusercontent.com/tylerdel30-dev/aero-os/main/catalog.json
